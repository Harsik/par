server:
  port: 8088
logging:
    level:
        root: INFO
        sample:
            mybatis:
                mapper: TRACE
mybatis:
    config-location: classpath:mybatis-config.xml
spring:
    h2:
        console:
            enabled: true
            path: /console
    datasource:
        driver-class-name: org.h2.Driver 
        url: jdbc:h2:file:~/Github/h2/test_db;AUTO_SERVER=TRUE
        username: sa
        password: 
